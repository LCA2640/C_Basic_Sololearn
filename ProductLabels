/* You are making an app to print product labels, which include the name and the price of the products.
The given code has the wrong format specifiers. Fix them to get the required output, corresponding to the given values.

#include <stdio.h>
int main() {
    printf("Product: %d \n", "LCD Monitor");
    printf("Price: %c", 49.95);
    return 0;
}
*/

#include <stdio.h>

int main() {
    printf("Product: %s \n", "LCD Monitor");
    printf("Price: %f", 49.95);

    return 0;
}
